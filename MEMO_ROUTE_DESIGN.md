# 메모 라우팅 설계

## 결정

메모 목록, 생성, 상세 경로를 `/memos` 아래로 통합한다.

```text
/memos             메모 목록
/memos/new         새 메모 생성 명령
/memos/:memoId     메모 상세 편집
```

기존의 `/memo`와 `/memo/:memoId` 프론트 경로는 제거한다.

## 변경 의도

기존 구조에서는 목록은 `/memos`, 생성과 상세는 `/memo`에 있어 같은 메모 리소스가 두 개의 루트 경로로 나뉘었다.

```text
/memos             목록
/memo              자체 화면 없이 /memo/new로 이동
/memo/new          생성
/memo/:memoId      상세
```

특히 `/memo`의 index loader는 표시할 화면 없이 `/memo/new`로 이동하여 새 메모 생성을 간접적으로 실행했다. 이 중간 경로는 사용자에게 별도 의미가 없고, `/memo` 접근만으로 새 메모가 생성되는 동작도 명확하지 않았다.

통합 후 `/memos`는 목록을 직접 표시하고, 새 메모 생성은 명시적인 `/memos/new`에서만 실행한다.

## 새 메모 생성 흐름

`/memos/new`는 작성 화면이 아니라 생성 명령 경로다.

```text
/memos/new 진입
  → 제목과 본문이 빈 메모를 POST
  → 응답에서 memoId 확인
  → /memos/:memoId로 replace 이동
  → 이후 편집 내용은 PATCH로 자동 저장
```

`replace`를 사용하는 이유는 뒤로 가기로 `/memos/new`에 다시 진입하여 메모가 중복 생성되는 것을 막기 위해서다.

## 레거시 경로 호환

현재 서비스는 QA 단계이므로 기존 `/memo/:memoId` 주소에 대한 외부 공유 링크 호환은 제공하지 않는다. 기존 주소를 새 주소로 보내는 레거시 redirect도 추가하지 않는다.

서비스 출시 후 공개 URL을 변경해야 한다면 브라우저 북마크, 방문 기록, 메신저와 문서에 공유된 링크를 보호하기 위해 이전 경로의 redirect를 별도로 검토한다.

## 변경하지 않는 범위

이번 결정은 프론트엔드 페이지 URL에만 적용한다.

- 백엔드 API의 `/api/v1/memo` 경로는 변경하지 않는다.
- `pages/memo`와 `pages/memos` 폴더는 상세와 목록이라는 서로 다른 책임을 가지므로 합치지 않는다.
- 메모 타입과 서버 쿼리 키는 URL 구조와 무관하므로 변경하지 않는다.
