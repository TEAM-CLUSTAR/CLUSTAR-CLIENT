import { Icon } from '@cds/icon';

import LabelSelect from '@features/memo/label-select';

const MemoPage = () => {
  return (
    <div style={{ backgroundColor: 'grey' }}>
      <Icon name="ic_ai_gra" />
      <LabelSelect />
    </div>
  );
};

export default MemoPage;
